#include<iostream>
#include<vector>
#include<map>
#include<string>

using namespace std;

bool compare(map<pair(string,int)> s1, map<string, int> s2)
{
	return s1 >
}



int main()
{
	
	int N = 0;
	cin >> N;
	vector<int> K(N);
	map<string, int> message;
	for(int i=0;i<N;i++)
	{
		cin >> K[i];
		for(int j=0;j<K[i];j++)
		{
			string tempstr;
			int tempint;
			cin >> tempstr >> tempint;
		}
	}

	cout << message.size() << endl;





	return 0;
}